/** @type {import('next').NextConfig} */
const nextConfig = {
    // transpilePackages: ['three'],
    experimental: {
        appDir: "experimental",
        
      },
}


module.exports = nextConfig
